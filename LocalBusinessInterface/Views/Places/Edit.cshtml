@model LocalBusinessInterface.Models.Place

<div class="row">
  <div class="col-md-6 offset-md-3">
    <h1 class="text-center">Edit Place</h1>
    view bag = @ViewBag.boardId
    @using (Html.BeginForm())
    {
      <input type="hidden" name="categoryId" value="@ViewBag.categoryId">
      <div class="form-group">
        <p>Place Title</p>
        @Html.TextBoxFor(model => model.Title, new {@class="form-control"})
      </div>
      <div class="form-group">
        <p>Place Body</p>
        @Html.TextBoxFor(model => model.Body, new {@class="form-control"})
      </div>
      <div class="form-group">
        <p>Picture</p>
        @Html.TextBoxFor(model => model.ImageSource, new {@class="form-control"})
      </div>
      <button type="submit" class="btn btn-info btn-block"> Edit a Place </button>
    }
  </div>
</div>